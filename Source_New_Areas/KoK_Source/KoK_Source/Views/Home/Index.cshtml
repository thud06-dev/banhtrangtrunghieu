@model KoK_Source.Models.HomeModel
@using KOKService;
@{ 
    KOK_DATAEntities _kokDataEntity = new KOK_DATAEntities();
    var dt = _kokDataEntity.KOK_BANNER.Where(a => a.ACTIVE == true && a.BANNER_TYPE == 3).OrderByDescending(a=>a.UPDATE_DATE).Take(2).ToList();
}
<div class="header_bot container" style="padding-left:0px">
   @Html.Partial("~/Views/Shared/_slider.cshtml")
</div>
<!---------ads_1--------------->
    <section class="ads_1">
        @if (dt.Count > 0)
        {
            <div class="ads_1-left"><a><img width="477" height="151" src="@Html.Raw(Request.Url.GetLeftPart(UriPartial.Authority)+Url.Content(dt.First().BANNER_FILE))" alt="" /></a></div>
            <div class="ads_1-right"><a><img width="477" height="151" src="@Html.Raw(Request.Url.GetLeftPart(UriPartial.Authority)+Url.Content(dt.Last().BANNER_FILE))" alt="" /></a></div>
        }
        
    </section>
    <!---------ads_1--------------->
    <section class="contents">
        <div class="contents_left contents_lefts">
            @Html.Partial("_contentProducts", Model.ListProducts)
        </div>
        <div class="contents_right">
            @Html.Partial("_sidebarTinTuc",Model.ListNews)
            @Html.Partial("_SPBanChay")
        </div>
    </section>